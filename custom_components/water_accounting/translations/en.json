{
  "title": "Water Accounting",
  "config": {
    "step": {
      "user": {
        "title": "Water Accounting",
        "description": "Add the read-only NikaS water accounting panel backed by existing Home Assistant entities."
      }
    },
    "abort": {
      "already_configured": "The panel is already configured."
    }
  }
}

