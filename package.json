{
  "name": "ha-water-accounting",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check && node scripts/check-panel.mjs && python -m unittest discover -s tests -v"
  }
}
